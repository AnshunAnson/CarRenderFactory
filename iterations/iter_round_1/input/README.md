# Iteration Round 1 Input Snapshot

Round 1 is the bootstrap round, so there is no prior `iter_round_{N-1}` artifact bundle.

To satisfy traceability, this round uses the repository `HEAD` state before modifications as the baseline input.
